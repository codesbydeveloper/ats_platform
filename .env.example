# Copy to `.env.local` for local dev, or set in your host (Vercel, etc.)
# No trailing slash — app calls https://<host>/api/...
NEXT_PUBLIC_API_URL=https://ats.raomtech.com

# Local backend example:
# NEXT_PUBLIC_API_URL=https://ats.raomtech.com
